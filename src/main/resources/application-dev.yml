spring:
  datasource:
    url: jdbc:h2:mem:test;INIT=create domain if not exists clob as text;MODE=PostgreSQL
    username: sa
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  h2:
    console:
      enabled: true
      path: /h2-console