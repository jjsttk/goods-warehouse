spring:
  datasource:
    url: jdbc:h2:mem:test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console