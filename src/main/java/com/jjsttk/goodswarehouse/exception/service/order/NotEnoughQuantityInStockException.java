package com.jjsttk.goodswarehouse.exception.service.order;

/*
public class NotEnoughQuantityInStockException extends RuntimeException {
    private static final String EXCEPTION_MESSAGE_FORMAT_LIST = "Not enough quantity in stock for products with id's = %s";
    private static final String EXCEPTION_MESSAGE_FORMAT = "Not enough quantity in stock for product with id = %s";

    public NotEnoughQuantityInStockException(List<UUID> productIds) {
        super(String.format(EXCEPTION_MESSAGE_FORMAT_LIST, productIds));
    }

    public NotEnoughQuantityInStockException(UUID productId) {
        super(String.format(EXCEPTION_MESSAGE_FORMAT, productId));
    }
}

 */
